server:
  port: 8081
spring:
  main:
    web-application-type: servlet
  datasource:
    url: jdbc:postgresql://localhost:5432/MyRhRecrutement
    username: admin
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.gmail.com
    port: 587
    username: uanemaro216@gmail.com
    password: hgze mqkn dbht gpzh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true